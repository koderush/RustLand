# RustLand
A Rust playground.

# Tool Chain
`sudo apt update`

`sudo apt install gcc`

`sudo apt install pkg-config`

`sudo apt install libssl-dev`

# Run
`cargo run`

# Debug
Need to install `CodeLLDB`